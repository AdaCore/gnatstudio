with "gtkada";
with "../kernel/kernel";
with "../shared";
with "core/toolchains_core";

project Toolchains_UI is

   for Languages use ("Ada");
   for Source_Dirs use ("src");
   for Object_Dir use Shared'Object_Dir;

   package Compiler renames Shared.Compiler;
   package IDE renames Shared.IDE;

end Toolchains_UI;
