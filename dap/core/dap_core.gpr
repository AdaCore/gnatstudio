with "../../common/common";
with "../../kernel/kernel_core";
with "../../shared";
with "vss_json";
--  Reuse the Minimal_Perfect_Hash generic shipped with ALS
with "lsp_base";

project DAP_Core is

   for Languages use ("Ada");
   for Source_Dirs use ("src",
                        "../generated");
   for Object_Dir use "../../obj/dap_core";

   package Compiler extends Shared.Compiler is
      for Switches ("Ada") use Shared.Compiler'Switches ("Ada");
   end Compiler;

end DAP_Core;
