*** configure.old	Thu May 23 23:20:40 2002
--- configure	Wed Jun 12 18:13:21 2002
***************
*** 13989,13995 ****
  
  IFS="${IFS= 	}"; gtk_save_ifs="$IFS"; IFS=","
  for loader in $included_loaders; do
!  if echo "$all_loaders" | grep "\(^\|\,\)$loader\(\$\|\,\)" > /dev/null; then
     :
   else
     { { echo "$as_me:$LINENO: error: the specified loader $loader does not exist" >&5
--- 13989,13995 ----
  
  IFS="${IFS= 	}"; gtk_save_ifs="$IFS"; IFS=","
  for loader in $included_loaders; do
!  if echo "$all_loaders" | grep -E "(^|,)$loader($|,)" > /dev/null; then
     :
   else
     { { echo "$as_me:$LINENO: error: the specified loader $loader does not exist" >&5
***************
*** 14160,14172 ****
  
  if $dynworks ; then
    STATIC_LIB_DEPS=
!   if echo "$included_loaders" | grep "\(^\|\,\)tiff\(\$\|\,\)" > /dev/null; then
      STATIC_LIB_DEPS="$STATIC_LIB_DEPS $LIBTIFF"
    fi
!   if echo "$included_loaders" | grep "\(^\|\,\)jpeg\(\$\|\,\)" > /dev/null; then
      STATIC_LIB_DEPS="$STATIC_LIB_DEPS $LIBJPEG"
    fi
!   if echo "$included_loaders" | grep "\(^\|\,\)png\(\$\|\,\)" > /dev/null; then
      STATIC_LIB_DEPS="$STATIC_LIB_DEPS $LIBPNG"
    fi
  else
--- 14160,14172 ----
  
  if $dynworks ; then
    STATIC_LIB_DEPS=
!   if echo "$included_loaders" | grep -E "(^|,)tiff($|,)" > /dev/null; then
      STATIC_LIB_DEPS="$STATIC_LIB_DEPS $LIBTIFF"
    fi
!   if echo "$included_loaders" | grep -E "(^|,)jpeg($|,)" > /dev/null; then
      STATIC_LIB_DEPS="$STATIC_LIB_DEPS $LIBJPEG"
    fi
!   if echo "$included_loaders" | grep -E "(^|,)png($|,)" > /dev/null; then
      STATIC_LIB_DEPS="$STATIC_LIB_DEPS $LIBPNG"
    fi
  else
*** modules/input/Makefile.in.old	Thu May 23 23:38:10 2002
--- modules/input/Makefile.in	Wed Jun 12 18:25:59 2002
***************
*** 782,792 ****
  install-data-local:
  	if [ -z "$(DESTDIR)" ] ; then \
  	  $(mkinstalldirs) $(DESTDIR)$(sysconfdir)/gtk-2.0 ; \
! 	  $(top_builddir)/gtk/gtk-query-immodules-2.0 > $(DESTDIR)$(sysconfdir)/gtk-2.0/gtk.immodules ; \
          fi
  
  gtk.immodules: Makefile.am $(module_LTLIBRARIES)
! 	$(top_builddir)/gtk/gtk-query-immodules-2.0 *.la > gtk.immodules
  
  all-local: gtk.immodules
  
--- 782,792 ----
  install-data-local:
  	if [ -z "$(DESTDIR)" ] ; then \
  	  $(mkinstalldirs) $(DESTDIR)$(sysconfdir)/gtk-2.0 ; \
! 	  touch $(DESTDIR)$(sysconfdir)/gtk-2.0/gtk.immodules ; \
          fi
  
  gtk.immodules: Makefile.am $(module_LTLIBRARIES)
! 	touch $(DESTDIR)$(sysconfdir)/gtk-2.0/gtk.immodules ; \
  
  all-local: gtk.immodules
  
