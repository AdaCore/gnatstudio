with "../gtkada/gtkada";
with "../kernel/kernel";
with "../widgets/widgets";

project Navigation is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";

   package Compiler is
      for Default_Switches ("Ada") use ("-a", "-g", "-O2", "-gnaty", "-gnatwue");
   end Compiler;

end Navigation;
