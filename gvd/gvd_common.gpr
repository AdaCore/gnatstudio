with "../gtkada/gtkada";
with "gnat_gvd";

project GVD_Common is

  for Source_Dirs use ("common");
  for Object_Dir use "gvd";

   package Compiler is
      for Default_Switches ("Ada") use ("-g", "-O2", "-gnaty", "-gnatwue");
   end Compiler;

end GVD_Common;
