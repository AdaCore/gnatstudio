CC = @CC@
CFLAGS = -g -O2
ALL_CFLAGS=$(CFLAGS) @DEFS@ -DSYSTEM_INCLUDE=@opsysfile@

all: gexpect_tty.o g-exptty.o
static: all

g-exptty.o: g-exptty.adb g-exptty.ads g-expect.ads
	$(CC) -c -gnatg $(CFLAGS) g-exptty.adb

gexpect_tty.o: gexpect_tty.c systty.h Makefile
	$(CC) -c $(ALL_CFLAGS) $<

install:

clean:
	-$(RM) *.o
	-$(RM) *.ali
	-$(RM) *~
	-$(RM) b~*

mostlyclean: clean

distclean: mostlyclean
	-$(RM) Makefile

maintainer-clean: distclean

