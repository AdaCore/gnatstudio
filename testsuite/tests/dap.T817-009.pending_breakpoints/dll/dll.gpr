
library project DLL is

   for Languages use ("Ada");
   for Library_Dir use "..";
   for Library_Kind use "relocatable";
   for Library_Name use "adadll";
   for Library_Interface use ("p");

   package Compiler is
      for Switches ("Ada") use ("-g");
   end Compiler;

end DLL;
