foo(bar)
{
	// Comment
	bar(A::B);

  A << static_cast<unsigned char> (A);
  A << B;
  A << static_cast<unsigned char> (A);
  A << static_cast<unsigned char> (A);
  A << static_cast<unsigned char> (A);
  B << static_cast<unsigned char> (A);
  B << static_cast<unsigned char> (A);
  B << static_cast<unsigned char> (A);

	while (True) // Comment
	{ // Comment
		bar(<< "Hello World" << "Foo" << "Bar" << endl);
	}
  delete foo; // foo bar
}
