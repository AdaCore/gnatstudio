project Default is

   for Languages use ("Ada");
   for Source_Dirs use (".");
   for Main use ("main.adb");

   package Compiler is
      for Switches ("ada") use ("-gnatL", "-save-temps", "-g", "-gnata", "-fcallgraph-info=su,da");
   end Compiler;

end Default;

