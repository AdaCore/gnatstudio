with "gtkada";
with "../shared";
with "../plugins";

with "../action_editor/action_editor";
with "../ada_module/ada_module";
with "../aliases/aliases";
with "../aunit/aunit";
with "../browsers/browsers";
with "../builder/builder";
with "../code_analysis/code_analysis";
with "../codefix/codefix";
with "../completion/completion";
with "../cpp_module/cpp_module";
with "../custom/custom";
with "../distrib";
with "../docgen2/docgen2";
with "../docs/docs";
with "../toolchains/toolchains";
with "../gvd/gvd";
with "../help/help";
with "../kernel/kernel";
with "../keymanager/keymanager";
with "../navigation/navigation";
with "../prj_editor/prj_editor";
with "../python/python";
with "../refactoring/refactoring";
with "../remote/remote";
with "../shell/shell";
with "../socket/socket";
with "../src_editor/src_editor";
with "../theme_manager/theme_manager";
with "../vcs/vcs";
with "../vdiff/vdiff";
with "../views/views";
with "../vfs/vfs";
with "../vsearch/vsearch";

project GPS is

   for Languages use ("Ada");
   for Main use ("gps-main.adb");

   for Source_Dirs use ("src");
   for Object_Dir use "obj";

   package Builder is
      for Executable ("gps-main.adb") use "gps";

      case Shared.Build is
         when "Debug" =>
           for Default_Switches ("Ada") use ("-m", "-g");
           for Global_Configuration_Pragmas use "gnat_debug.adc";

         when "Production" =>
           for Default_Switches ("Ada") use ("-m", "-g");
           for Global_Configuration_Pragmas use "gnat.adc";
      end case;
   end Builder;

   package Compiler renames Shared.Compiler;

   package Binder is
      for Default_Switches ("Ada") use ("-E");
   end Binder;

   package Linker is
      case Shared.OS is
         when "Windows_NT" =>
            for Default_Switches ("Ada") use ("obj/gps.res");
         when "unix" =>
            null;
      end case;
      --  for Default_Switches ("Ada") use ("-lgmem");
      --  for Default_Switches ("Ada") use ("-pg");
   end Linker;

   package Naming is
      case Shared.OS is
         when "Windows_NT" =>
            for Implementation ("dde") use "dde_win32.adb";
         when "unix" =>
            for Implementation ("dde") use "dde_default.adb";
      end case;
   end Naming;

   package IDE renames Shared.IDE;

end GPS;
