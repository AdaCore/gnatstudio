with "../gtkada/gtkada";
with "../common/common";
with "../kernel/kernel";

project VDiff is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";

   package Compiler is
      for Default_Switches ("Ada") use ("-g", "-O2", "-gnaty", "-gnatwue");
      for Switches ("vdiff_pkg.adb") use ("-g", "-O2");
   end Compiler;

end VDiff;
