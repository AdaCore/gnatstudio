with "../gtkada/gtkada";
with "../gvd/gvd_common";
with "../common/common";

project Widgets is

   for Languages use ("C", "Ada");
   for Source_Dirs use ("src", "cschtml");
   for Object_Dir use "obj";

   package Compiler is
      for Default_Switches ("Ada") use ("-g", "-O2", "-gnaty", "-gnatwue");
   end Compiler;

end Widgets;
