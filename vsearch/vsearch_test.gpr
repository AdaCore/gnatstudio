with "../gtkada/gtkada";
with "../gvd/gvd_common";
with "../common/common";
with "vsearch";

project Vsearch_Test is

   for Source_Dirs use ("tests");
   for Object_Dir use "obj";

   package Compiler is
      for Default_Switches ("Ada") use ("-gnata", "-g", "-O2", "-gnaty", "-gnatwue");
   end Compiler;

end Vsearch_Test;
