with "../gnat";
with "../gtkada/gtkada";
with "../common/common";
with "../widgets/widgets";
with "../gvd/gvd";

project Kernel is

   for Source_Dirs use ("src", "tests_src_info");

   for Object_Dir use "obj";

   package Compiler is
      for Default_Switches ("Ada") use
        ("-g", "-gnata", "-O2", "-gnaty", "-gnatwu", "-gnatwe");
   end Compiler;

end Kernel;
