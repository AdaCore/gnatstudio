--  Provide dependencies on the Prj hierarchy needed within Glide.

pragma Warnings (Off);
with Prj.Ext;
with Prj.Part;
with Prj.Proc;
with Src_Editor_Box;
pragma Warnings (On);

package Kernel is
end Kernel;
