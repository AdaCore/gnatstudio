--  Dummy package to provide a main file to compile that contains no
--  dependencies

pragma Warnings (Off);
with Prj.Ext;
with Prj.Part;
with Prj.Proc;
pragma Warnings (On);

package Kernel is
end Kernel;
