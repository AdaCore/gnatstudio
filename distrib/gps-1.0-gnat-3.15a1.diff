*** shared.gpr.old	Tue Oct  1 18:21:51 2002
--- shared.gpr	Tue Oct  1 18:22:03 2002
***************
*** 4,10 ****
     OS : OS_Type := External ("OS", "unix");
  
     type Build_Type is ("Debug", "Production");
!    Build : Build_Type := External ("Build", "Debug");
  
     for Source_Dirs use ();
  
--- 4,10 ----
     OS : OS_Type := External ("OS", "unix");
  
     type Build_Type is ("Debug", "Production");
!    Build : Build_Type := External ("Build", "Production");
  
     for Source_Dirs use ();
  
--- glide/gps.gpr	21 Oct 2004 15:05:13 -0000	1.65
+++ glide/gps.gpr	2 Nov 2004 17:08:37 -0000
@@ -39,11 +39,9 @@
       case Shared.Build is
          when "Debug" =>
            for Default_Switches ("Ada") use ("-m", "-a", "-g");
-           for Global_Configuration_Pragmas use "gnat_debug.adc";
 
          when "Production" =>
            for Default_Switches ("Ada") use ("-a", "-g");
-           for Global_Configuration_Pragmas use "gnat.adc";
       end case;
    end Builder;
 
@@ -52,9 +50,10 @@
    package Binder is
       case Shared.OS is
          when "Windows_NT" =>
+            for Default_Switches ("Ada") use ("-t");
 
          when "unix" =>
-            for Default_Switches ("Ada") use ("-E");
+            for Default_Switches ("Ada") use ("-E", "-t");
       end case;
    end Binder;
 
