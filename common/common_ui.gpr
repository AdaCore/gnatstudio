with "gtkada";
with "common";
with "gnatcoll_gtk";

project Common_UI is

   for Languages use ("Ada");
   for Source_Dirs use ("ui/src");
   for Object_Dir use "ui/obj";

   package Compiler is
      for Switches ("Ada")
        use Common.Compiler'Switches ("Ada");
   end Compiler;

   package IDE renames Common.IDE;

end Common_UI;
