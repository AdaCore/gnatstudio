with "../../gnat";
with "../../common/common";

project Convert is
   for Main use ("convert_prj");
   for Source_Dirs use (".", "../../prj_editor/src", "../../kernel/src_info",
                        "../../kernel/src");
   for Object_Dir use "obj";
   for Exec_Dir use ".";
   
   package Compiler is
      for Default_Switches ("Ada") use
         ("-gnaty", "-gnatQ", "-g", "-gnatwjmeurk");
   end Compiler;

end Convert;
